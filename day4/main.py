import sys
import math

if __name__ == '__main__':

    # # Saves input as string
    # input = sys.stdin.read().strip()
    # print(type(input))

    # # Iterates over chars of the string
    # for s in input:
    #     print(s)

    # Saves a list of strings containing the lines
    input2 = sys.stdin.readlines()
    # print(type(input2))

    # Iterates over the lines in the list
    for s in input2:
        print(s)
